import { Doctor } from '@schemedesigns/doctor';
import { Patient } from '@schemedesigns/patient';
import { Scheduler } from '@schemedesigns/scheduler';

console.log(new Doctor());
console.log(new Scheduler());
console.log(new Patient());
